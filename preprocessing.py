def query_preprocessor(query):

    pp_query = query.lower()
    pp_query = pp_query.strip()
    # print(pp_query)
    # pp_query = re.split(r'\s+|[,;?!.-]\s*', pp_query)
    return pp_query

    

    


